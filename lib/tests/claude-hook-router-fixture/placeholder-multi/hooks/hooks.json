{
  "hooks": {
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/bin/run --root ${CLAUDE_PLUGIN_ROOT}/data",
            "contract": "observe"
          }
        ]
      }
    ]
  }
}
