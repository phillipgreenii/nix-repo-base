{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^(Read|Grep)$",
        "hooks": [
          { "type": "command", "command": "echo regex", "contract": "observe" }
        ]
      }
    ]
  }
}
